import styled from "styled-components";

export const UpdatePage = styled.div`
   .tokenInput {
     display: flex;
     align-items: center;

     .input {
       flex: 1;
       input {
        height: 48px;
       }
     }
   }
`